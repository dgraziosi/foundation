# Evidence

Proof for a verification run lives in a child folder named with `VERIFY_RUN_ID`:

```text
.cursor/skills/verify-foundation/evidence/<run-id>/
```

Cleanup must not delete these folders. Do not put API keys, `.env`, cookie values, or vault rows here. Redact secrets. A first-day empty vault needs no titles.
