# Retired-words lock fixture

This file is not a product skill. The layout tester greps it so the
retired-word check still fails a real line. Do not ship these as live keys.

search `{ living }`
data.living
search `{ code }`
data.code
search `{ link }`
data.link
current picture
